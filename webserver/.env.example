# Docker Compose 环境变量（仅示例文件，实际部署请用安全随机值）
# 授权码
AUTH_CODE=your_secure_code_here
# JWT 秘钥
JWT_SECRET=your_jwt_secret_here

# 可选：覆盖服务器数据目录（SQLite 数据库、图标、Favicon 缓存等）
# 默认值：生产环境为 /app/data，开发环境为 webserver/server/data
# DATA_DIR=./data

# 可选：CORS 允许的来源名单，逗号分隔，例如 https://neutab.example.com,chrome-extension://<id>
# 也支持简单前缀通配：chrome-extension://*
# CORS_ORIGIN=

# 可选：Favicon 缓存的生存时间（秒），默认 30 天（2592000 秒）
# FAVICON_CACHE_TTL_SECONDS=2592000

# 可选：Favicon 负缓存（上游 404）生存时间（秒），默认 1 小时（3600 秒）
# FAVICON_NEGATIVE_CACHE_TTL_SECONDS=3600

# 可选：Favicon 上游请求超时（毫秒），默认 8000
# FAVICON_UPSTREAM_TIMEOUT_MS=8000

# 可选：同步及图标上传允许的最大图标大小（字节），默认 1048576
# MAX_ICON_BYTES=1048576

# 可选：/api/sync 接口每次允许的最大 key 数量，默认 200
# MAX_SYNC_KEYS=200

# 可选：/api/sync 接口单个 value 最大 JSON 编码字节数，默认 2097152
# MAX_SYNC_VALUE_BYTES=2097152

# 可选：如果部署在反向代理（Nginx/Caddy/Cloudflare）后面，设置为 1 以正确获取客户端 IP
# TRUST_PROXY=1
