services:
  neutab:
    build:
      context: ..
      dockerfile: webserver/Dockerfile
    ports:
      - "3001:3001"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
